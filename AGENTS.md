# Project Rules

## 記事作成

新しい記事は `make new-article` で作成する。記事ファイルと画像ディレクトリ（`/images/<slug>/`）が同時に生成される。
slug は `zenn-cli` が生成したランダムな文字列のまま維持する。

## 記事執筆ルール

### 文章規範

記事の本文を書く・推敲する前に、リポジトリルートの `.claude/skills/japanese-tech-writing/SKILL.md` を読み込み、その規範に従って書く。
加えて、このリポジトリでは次に従う。

- 「です・ます」調で統一する
- 一文は長くても80文字程度を目安にする
- 結論や要点を先に書き、詳細は後に続ける

### frontmatter

- title, emoji, type, topics, published を必ず含める
- topics は最大5つまで、すべて小文字で書く（例: `Next.js` ではなく `nextjs`）
- `published` はユーザーの明示的な指示がない限り変更しない

### Zenn のマークダウン記法

- 見出しは h2（`##`）から始める（h1 は title が担う）
- コードブロックにはファイル名を付ける: ` ```js:index.js `
- リンクカードにしたい URL は単独行に置く
- 数式は KaTeX 記法: `$inline$` / `$$block$$`
- アコーディオン: `:::details タイトル`
- メッセージボックス（`:::message` / `:::message alert`）は原則使わない。派手な色の囲みになるため、ちょっとした内容に使うと過剰な表現になる。データが消える、自己責任が伴うなど、本当に注意が必要な内容だけに使う

## 画像

- 画像は `/images/<記事のslug>/` 配下に置き、3MB 以下にする
- ファイル名は内容がわかる具体的な名称にする（例: `main-play-screen.png`, `architecture-diagram.png`）。スクリーンショットのデフォルト名（例: `2026-02-20 3.32.58.png`）のまま保存しない
- 参照は絶対パスで書き、alt テキストを付ける: `![代替テキスト](/images/<slug>/filename.png)`
- ユーザーが画像を追加しようとしている場合は、上記に沿った配置とリネームを提案する
